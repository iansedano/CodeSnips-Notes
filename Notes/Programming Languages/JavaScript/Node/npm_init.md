```powershell
node -v
npm -v
```

Start

```
npm init
or
npm init --y
```

To install packages:

```
npm install package
```

This creates a lockfile

When moving project to another computer
```
npm i
```

To uninstall

```
npm uninstall package
```